# Cycle 13 Standalone Device Review Candidate — Unapproved

These files are **UNAPPROVED review candidates only**. They are not integrated into the website, do not replace any active media, and must not be used to build responsive derivatives without owner approval. The raw candidate was created in one reference-guided image-edit attempt using the built-in default image refinement route and `client/public/media/bns-cycle7-device-final_0e062148.jpg` as the local reference. `bns-palm-scale-device-review.png` is the exact raw generator output at **1632 × 2176 px**. `bns-palm-scale-device-side-by-side.jpg` is a deterministic comparison at **2880 × 1920 px**, showing the current Cycle 7 standalone device master on the left and the raw candidate scaled to 1440 × 1920 px on the right.
